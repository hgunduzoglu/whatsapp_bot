{
	email {$ACME_EMAIL}
}

# Backend API (WhatsApp webhook lives here)
{$API_DOMAIN} {
	reverse_proxy api:3000
}

# Admin panel (SPA served by its own nginx container)
{$ADMIN_DOMAIN} {
	reverse_proxy admin:80
	encode gzip
}
